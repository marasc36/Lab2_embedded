#include <msp430.h> 


/**
 * main.c
 */

int main(void)
{
    WDTCTL = WDTPW | WDTHOLD;   // stop watchdog timer



   int a = 0b10101001001;
   int b = 0b10111011011;

   int d = sum(a,b);

   while(1);
   return(0);
}

int sum (int s1, int s2){
    int c;
    c = s1+s2;
    return z;
}
